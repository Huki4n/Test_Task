import exitButton from '../../../assets/ExitButton/ExitButton.svg'

const ExitButton = () => {
  return <button>
    <img src={exitButton} alt={'Exit Button'}/>
  </button>
}

export default ExitButton;